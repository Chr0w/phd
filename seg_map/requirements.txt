# Runtime extras for Isaac Sim's Python (omni, pxr, isaacsim, rclpy come from Isaac Sim / ROS2):
#   $HOME/isaacsim/python.sh -m pip install -r requirements.txt
#
# Dev/IDE tooling is defined in pyproject.toml:
#   pip install -e ".[dev]"
numpy>=1.26.0
pyyaml>=6.0
